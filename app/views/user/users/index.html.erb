<%= link_to 'Log out', destroy_user_session_path, :method => :delete %>
<p>
<% @flats.each do |flat| %>
  country: <%= flat.country %>,
  city: <%= flat.city %>,
  street: <%= flat.street %>,
  house: <%= flat.home_number %>,
  flat: <%= flat.flat_number %>
  <p>
<% end %>

<%= will_paginate @flats %>
